[project]
name = "git-scripts"
version = "0.1.0"
description = "Scripts for git, such as automatically resolving merge conflicts"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pymarkdownlnt>=0.9.33",
    "ty>=0.0.1a31",
]
